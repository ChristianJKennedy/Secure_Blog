<h1>Edit Profile</h1>


<%= form_for @user, url: profile_path(@user), method: :patch do |f| %>
  <div class="field">
    <%= f.label :bio %>
    <%= f.text_area :bio %>
  </div>
  <%= f.submit "Save", class: "btn btn-primary" %>
<% end %>

